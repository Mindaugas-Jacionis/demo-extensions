import { NAME } from './constants';

export const GET_WEATHER = `${NAME}.GET_WEATHER`;
export const GET_WEATHER_SUCCESS = `${NAME}.GET_WEATHER_SUCCESS`;
export const GET_WEATHER_FAILURE = `${NAME}.GET_WEATHER_FAILURE`;
